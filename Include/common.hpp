#pragma once

#include <cmath>
#include <vector>
#include <list>
#include <string>
#include <sstream>
#include <iostream>

#define MIN(x, y) (x < y ? x : y)
#define MAX(x, y) (x > y ? x : y)

#define M_PI 3.14159265359f
#define M_PI_2 1.57079632679f
